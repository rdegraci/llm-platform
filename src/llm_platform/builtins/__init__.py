"""Optional built-in plugins (opt-in via config ``plugins``).

These are generic helpers, not domain integrations. They stay off until an app
lists them under ``plugins:`` (for example ``time``, ``todo``).
"""
